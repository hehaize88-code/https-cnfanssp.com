# hacoovip.com

Independent Hacoo spreadsheet, finds, QC, shipping, returns and review guide.

## Build

```bash
npm ci
npm run build
```

The static Cloudflare Pages output is generated in `out/`.

Cloudflare Pages project: `hacoovip-com` (production branch: `main`).
